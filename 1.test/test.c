#include <stdio.h>

#define _USE_MATH_DEFINES
#include <math.h>

int main(){
    /*printf("%lf\n\n", M_PI);
    int userInput;
    int result = 0;
    while(userInput != 0){
        printf("Enter a number different than 0: ");
        scanf("%d", &userInput);
        result += userInput;
    }
    printf("Result: %d",result);*/
    char test [50];
    fgets(test,50,stdin);
    puts(test);
    return 0;
}
