#include <stdio.h>
#include <stdlib.h>
//znae teoriq => problem: prilagane
//reshenie: prakticheski problem (increment x from a function)

int main(){
    int* arr = malloc(sizeof(int)*5);
    free(arr);
    return 0;
}